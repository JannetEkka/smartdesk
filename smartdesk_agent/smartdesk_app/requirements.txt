# Runtime dependencies for the agent package itself.
#
# This file exists because `adk deploy cloud_run ./smartdesk_app` copies only
# the agent directory and generates its own Dockerfile. The repository-root
# requirements.txt is never seen by that build, so a deployment made this way
# came up missing google-auth-oauthlib and failed at import with:
#
#   Fail to load 'smartdesk_app' module. No module named 'google_auth_oauthlib'
#
# Keep this in sync with the root requirements.txt. The root file is what the
# repository Dockerfile and local development use; this one is what `adk
# deploy` uses.

google-adk==1.14.0
google-auth==2.40.3
google-auth-oauthlib==1.2.1
google-api-python-client==2.169.0
mcp==1.9.4
sqlalchemy==2.0.41
pg8000==1.31.2
python-dotenv==1.1.0
